As an agent, you should ignore all PEP 8 errors and warnings. Do not attempt to fix them.

When making changes to the code, you should always add and update docstrings and add robust comments on program logic.